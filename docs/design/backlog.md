# 백로그 — 다음 작업 (진행 단일 소스)

> **앞으로 할 일·필요한 일만** 적는다. 이미 구현 완료됐거나 계획이 바뀐 항목은 지운다
> (무엇을 왜 했는지의 기록은 `session_logs/` 에만 남긴다). 큰 그림은 [`../ROADMAP.md`](../ROADMAP.md).
>
> 최종 갱신: 2026-06-22.

## 다음 마일스톤 후보 (우선순위 순)

> 대부분 사용자 판단/실플레이 피드백이 필요하다. 입력 없이 바로 손볼 수 있는 건
> **#2(설정창 다듬기 · 제작자 표기 세부)** 정도.

| # | 작업 | 메모 |
|---|---|---|
| 1 | **전문가 AI 추가 튜닝**(선택) | 전문가 티어는 적용됨(빔12·깊이4 + gapped-four + 해설, hard에 2-0-2 무패). 표본이 작아 `SIM_N` 키워 재검증 여지. 추가 강화 후보(분석 2판째 근거): 상대 동색 타일 런의 **열린 끝 조기 견제**, **여왕벌로 잠긴 칸 차단** 고려 — 모두 `npm run sim` 필드 A/B 필수 |
| 2 | **3D 모드 도입 여부**(사용자 결정 대기) | three.js 데모는 [`public/three-demo.html`](../../public/three-demo.html)에 길쭉한 회전체 벌로 구현(`/three-demo.html`). 게임 본체 3D 전환은 별개 렌더 경로 + three 의존성 + 모델 자산(절차적 한계 → Blender GLTF 권장) 필요 — 큰 작업. 현재 shipping 비주얼은 SVG 귀여운 벌 |
| 3 | **설정창 다듬기 · 제작자 표기 세부**(선택) | 아코디언 적용됨. 아이콘·간격·반응형 세부 + 우하단 제작자 크레딧의 세부 디자인(최소판은 적용됨, 함께 진행하기로 함) |
| 4 | **벌집 배경 패턴 잔여**(선택) | 배경 무늬·파비콘·OG 적용됨. 남은 건 커서/버튼 꿀색 톤 마무리 정도 |
| 5 | **멀티플레이 로비**(미래·범위 밖) | 초대링크 방 + 로비 RoomSettings 공통. 엔진 직렬화/스냅샷 준비됨(공유 코드 BTB1 포함). **CLAUDE.md상 네트워킹은 아직 안 만듦** |
| - | **AI 평가 최적화**(선택) | `evaluate` 가 노드마다 보드 풀스캔·findLines 를 여러 번 반복(전문가 빔12·깊이4 + gapFour 로 ~3.7초/수). owner 맵 1회 생성·공유로 단축 가능 — 전문가 응답속도 개선 겸 |
