# 백로그 — 다음 작업 (진행 단일 소스)

> 합의됐으나 미착수/진행 중인 작업의 단일 소스. 다음에 뭘 할지는 여기서 본다.
> 단계 큰 그림은 [`../ROADMAP.md`](../ROADMAP.md). 완료 항목은 세션 로그로 내려보낸다.
>
> 최종 갱신: 2026-06-21

## 현재 마일스톤: 브라우저에서 핫시트 한 판을 끝까지

### 완료 ✅
- Phase 0 셋업(Vite+TS+Vitest, GitHub private 레포).
- `design/rules.md` v0.2 — 세부 규칙 6항목 확정.
- Phase 1 엔진: `hex / types / state / lines / hive / victory / moves / index`.
  단위 테스트 34개 통과, `check:engine` 통과. (커밋 `fe11411`)

### 다음 (우선순위 순)
| # | 작업 | 메모 |
|---|---|---|
| 1 | `src/ui` SVG 보드 렌더 | 육각 레이아웃(hex→픽셀), 타일/말/벌집 그리기 |
| 2 | 핫시트 입력·상태머신 | 타일 먼저→(타일 하나 더 \| 말 놓기) 분기, 첫 턴 ②강제, 타일소진 시 말만 |
| 3 | 상태바·승리/점수 배너 | 현재 턴·남은 자원·여왕벌 사용·결과 표시 |
| 4 | 새 게임 / 무르기(undo) | 상태 스택(엔진이 순수라 쉬움) |
| 5 | dev 서버 플레이 확인 → 커밋·푸시 | build/test/check:engine 통과 + 수동 플레이 |

## 그다음 (마일스톤 후보)
- Phase 3 vs AI: `engine/ai.ts` 인터페이스 + 그리디(승리수→차단→전개), UI 토글.
- 규칙 엣지케이스 테스트 보강(여왕벌 5목, 동시완성, 분기 벌집 점수).
- 모바일 터치/반응형, 애니메이션/사운드.

## 확정 필요 / 사용자 확인 대기
- **엔드게임 진행 세부**(엔진 구현 시 합리적 기본값으로 정함, `design/rules.md`에 명시 예정):
  타일 소진 후 말만 두는 구간, 양쪽 모두 둘 수 없을 때 점수 종료, 한쪽만 막혔을 때 패스.
  종이 설명서에 없던 부분 — 사용자가 다르게 원하면 조정.
