# 백로그 — 다음 작업 (진행 단일 소스)

> 합의됐으나 미착수/진행 중인 작업의 단일 소스. 다음에 뭘 할지는 여기서 본다.
> 단계 큰 그림은 [`../ROADMAP.md`](../ROADMAP.md). 완료 항목은 세션 로그로 내려보낸다.
>
> 최종 갱신: 2026-06-22.
> 참고: 아래 ✅ 항목의 "세션 N"은 누적(글로벌) 세션 번호이고, 일별 `session_logs/YYYY-MM-DD.md`
> 안의 "세션 N"은 그날 안에서의 순번이라 번호 체계가 다르다(같은 날짜라도 숫자가 다를 수 있음).

## ✅ 마일스톤 달성: 브라우저에서 핫시트 플레이 가능

- Phase 0 셋업(Vite+TS+Vitest, GitHub private 레포).
- `design/rules.md` v0.3 — §8 6항목 확정, §9 진행 세부(잠정).
- Phase 1 엔진: `hex/types/state/lines/hive/victory/moves/index`. (커밋 `fe11411`)
- 문서·운영 구조(INDEX/ROADMAP/backlog/known_issues + CLAUDE.md 운영규칙). (`7eb30b0`)
- Phase 2 핫시트 UI: SVG 보드·클릭 입력·턴 상태머신·승리/점수 배너·무르기. (`092be30`)
- 테스트 38개 통과(엔진 34 + UI 4), build/check:engine 통과.

### UI 1차 다듬기 ✅ (2026-06-21 세션 3)
- 패널 왼쪽 배치. 옅은 점선 배경 그리드(반경 12). 휠 줌 + 드래그 팬 + 키보드(화살표/＋－/0).
- 벌집 금색 글로우 오버레이로 가시성 강화. "뷰 리셋" 버튼.

### Phase 3 vs AI ✅ (2026-06-21 세션 4)
- `engine/ai.ts`: 승리→차단→Gomoku식 1수 평가, 난이도 Cfg seam(현재 medium 1종). (`4167d7e`)
- UI 모드 순환(사람/vs AI/AI 관전) + setTimeout 스케줄러. (`92d549b`)
- self-play 테스트로 엔진 자동 검증. 전체 50개 통과.

### AI 강화 + 피드백 + 배포 ✅ (2026-06-21 세션 5)
- AI 빔 서치(negamax+알파-베타, medium 깊이3) — 1수→여러 수 앞. medium>easy(4:2). (`034441e`)
- 직전 수 강조 + 리치(위험/승리) 힌트. `completingCells`/`winningCells` 공유. (`d940a5e`)
- 승리 5목 라인 강조(`winningLine`). (`00fc455`)
- **GitHub Pages 배포** — Actions 자동 배포, 레포 public 전환. (`11c41d9`)
  ▶ https://soomin007.github.io/be-the-bee/

### AI 허리끊기 + 설정/훈수 + 이펙트 ✅ (2026-06-21 세션 6)
- AI 평가에 허리 끊기(상대 발전 타일선 선점) 추가, hard 깊이4. hard 6:0 medium. (`09a30ee`)
- RoomSettings(모드/난이도/힌트/사운드) 중앙화 + 난이도 선택 + 훈수 모드(힌트 게이팅). (`1c06554`)
- Web Audio 효과음·등장 애니메이션·BGM 토글 + Suno 프롬프트. (`23eb994`)

### AI 수 생성 다양화 + 여왕벌 모드 ✅ (2026-06-21 세션 7)
- 수 생성 다양화: 기존/상대 타일 위 말(선점·허리 끊기), 타일 2개(①). 평가에 SEIZE. (`3e41ed7`)
  행동 테스트로 검증(기존타일위말·선점 발생). medium 4:2 easy, hard 4:0 medium.
- 여왕벌을 AI 에서 제거 + UI "여왕벌 모드" 토글(기본 꺼짐, 확장). (`3fb30d4`)

### 결과 모달 + 규칙 엣지테스트 + 모바일 ✅ (2026-06-21 세션 8)
- 결과 모달(승리/무승부·점수, 다시하기·닫기). (`3bfcdc5`)
- 규칙 엣지케이스 테스트(여왕벌 5목·동시완성 승리우선·교차 벌집·타일소진). (`9680ac8`)
- 모바일 핀치 줌 + 터치 타깃. (`b6c5c12`)

### 클릭 버그 수정 + 인게임 행동 바 + 직전 수 구분 ✅ (2026-06-21 세션 9)
- 보드 클릭 안 먹힘 수정(포인터 캡처) + Playwright E2E 검증 도구. (`968a112`, `7f206b4`)
- 인게임 ①/② 행동 바(보드 아래, 설정과 분리) + 키보드 1/2/Esc/Q.
- 직전 수: 타일=칸 점선, 말=말 둘레 링으로 구분.

### AI 튜닝 + 벌 테마 1단계 + BGM ✅ (2026-06-21 세션 10)
- AI 평가: 막힌 줄 무가치·포크 보너스·수비. hard 깊이5. (`78471e5`)
- 벌 테마 1단계: 말=벌(날개·줄무늬), 꿀색 배경. (`a40700d`)
- BGM 8곡(`public/bgm/`) 선택 + BGM/효과음 볼륨 슬라이더. (`cd2f5bd`)
- 무르기 버그 수정 + 모드/난이도 표시 + 설정 영속화(localStorage). (`645a523`)

### AI 성향·관전 제어 + 튜토리얼 + 설정 아코디언 + 정리 ✅ (2026-06-22)
- **AI 멈춤/데드락 버그 2건 수정**: 잠긴 벌집 승리 칸 오판(`02f5926`) + 막다른 위치 여왕벌
  최후 수단(`bd6e354`). `ai.placementMove`/`fallbackMove` 가 항상 합법수 보장.
- **AI 성향(persona)**(`06e99b1`): 균형/공격형/수비형/벌집형 — 평가 가중치 주입식 리팩터.
- **관전 제어**(`f4f3fb0`, `fe8e78d`): ▶시작/⏸멈춤, 색깔별 난이도·성향, 진영별 다른 시드,
  종료 시 자동 멈춤, 성향 한 줄 설명. → 매판 다른 전개로 관전 다양화.
- **첫 접속 튜토리얼**(`985cadd`, `bb9b89f`): 설명서 문구 그대로 8페이지 캐러셀(표지+규칙),
  도움말 섹션에서 재열기. 외부 에셋 0(게임과 같은 SVG).
- **설정 아코디언**(`6e098fd`): 게임/화면/AI·관전/사운드/도움말 섹션 접기·펼치기(영속화).
- **시각 다듬기/코드 정리**: 2.5D 그림자 광원 일관, 모드·난이도 버튼 현재값 표시, word-break
  단어 단위, em dash 전면 제거(`de5bf60`), 데드코드 제거(Cfg.noise/Candidate.at/void occupied).
- **AI 상성 분석**: self-play 시뮬레이션 → [`ai_strategy.md`](ai_strategy.md)(`npm run sim`).

### 컬러 테마 + 여왕벌 팝업 + 벌 연출/저장 + 2.5D ✅ (2026-06-22 세션 13)
- **컬러 테마 3종**(`src/ui/themes.ts`): 꿀(기본·가시성 개선)/고대비(색약·저시력)/벽돌.
  타일·말·벌집 색 분리, 그라데이션 동적 생성, 설정 순환 버튼 + 영속화. (`579098e`)
  → 사용자 지적 "벌집 색 구분 어려움" 대응: 노랑 배경 묻힘·명도 대비·벌집 글로우 개선.
- **여왕벌 모드 설명 팝업**(백로그 #1): 토글 켤 때 모달(확장 규칙 안내 + 확인/취소),
  확인해야 켜짐. `QUEEN_SVG` 마스코트. (`e20aa8b`)
- **벌 테마 2단계 잔여**(백로그 #4): 날갯짓 버즈음·벌집 완성 꿀 차오름(+완성음)·리치 붕붕 모션. (`37a327e`)
- **저장/불러오기 + 자동 이어하기**(백로그 #5): autosave(새로고침 복원) + 명시적 저장/불러오기 슬롯. (`ac0ec9d`)
- **말=벌 2.5D**(백로그 #2 부분): 바닥 그림자·구형 음영·하이라이트. 3D 모드 옵션은 보류. (`6bebf5b`)

### 인게임 UX 묶음 ✅ (2026-06-22 세션 12)
- 행동 바(턴 안내+①②)를 **보드 위/아래 선택** — 설정 "행동 버튼 ⬆위/⬇아래" 토글(기본 위),
  CSS `order` 로만 전환(DOM 유지)·localStorage 영속화.
- '뷰 리셋' → **'처음 위치로'** 로 순화(+툴팁). 키보드 안내도 "0 = 처음 위치로" 로 일관화.
- 패널 하단 도움말을 **`.help` 박스**로 묶어 가시성↑(글씨 진하게·줄간격·구분선·줄바꿈).
- `shot-action-pos.mjs`(상단/하단 PNG + 위치 전환 PASS/FAIL).

### AI 버그수정 + 복기 + 벌 테마 2단계 ✅ (2026-06-22 세션 11)
- AI 자기 승리수 누락 버그 수정(승리·차단을 후보 캡 무관 `winningCells`로 직접 점유) +
  난이도 깊이 재조정(easy 1수/medium 깊이2/hard 깊이4), FORK 22000→10000. (`884e6a3`)
- 관전 모드 수 간격(속도) 슬라이더. (`7faab2b`)
- **모든 모드 다시보기(복기)** — moveLog/timeline, 순수 뷰 오버레이, ⏮◀▶재생▶⏭ + 진행 슬라이더. (`6149d3b`)
- 벌 테마 2단계: 밀랍 타일 질감·놓기 꽃가루·승리 꿀 터짐·결과 모달 벌 마스코트. (`2156a59`)

## 다음 마일스톤 후보 (우선순위 순)

> 남은 건 대부분 **사용자 입력/판단이 필요한 것들**(실플레이 체감·범위 합의·규칙 확정).
> 입력 없이 바로 가능한 추천은 **#1(저장 슬롯 확장)**.

| # | 작업 | 메모 |
|---|---|---|
| 1 | **저장 슬롯 확장**(다음 추천) | 현재는 단일 저장 슬롯 + autosave. 여러 슬롯/이름·시간 표시·삭제, 기보 목록 UI. 입력 없이 진행 가능 |
| 2 | **AI 튜닝**(사용자 입력 필요) | 상성 분석은 [`ai_strategy.md`](ai_strategy.md)에 정리됨(수비형>균형≈공격형≫벌집형, hard>medium>easy). hard 체감·벌집형 약함 조정은 실플레이 피드백 받아 `npm run sim` 으로 A/B |
| 3 | **말=벌 3D 모드 옵션**(방향 합의 필요) | 2.5D는 적용됨. 설정에 2D/3D 분기는 범위 큼 — 착수 전 사용자와 방향 합의 |
| ~~4~~ | ~~벌집 배경 패턴·파비콘~~ ✅ | 완료: 벌집 무늬 배경 + 벌 파비콘/애플 아이콘 + OG 공유 미리보기(`e8bf2fb`) |
| 5 | **설정창 추가 다듬기**(선택) | 아코디언은 적용됨. 더 손본다면 아이콘·간격·반응형 등 세부 |
| 6 | **멀티플레이 로비**(미래·범위 밖) | 초대링크 방 + 로비 RoomSettings 공통. 엔진 직렬화/스냅샷 준비됨. **CLAUDE.md상 네트워킹은 아직 안 만듦** |
| 7 | **`rules.md §9` ⚠️ 확정**(사용자 확인 필요) | 타일 소진 후 진행 세부(잠정). 사용자 확인 후 ⚠️ 제거 |
| - | AI 평가 최적화(선택) | `evaluate` 가 노드마다 보드 풀스캔·findLines 를 여러 번 반복. owner 맵 1회 생성·공유로 단축 가능(현재 체감 지연 없음, 코드 감사 제안) |

## 확정 필요 / 사용자 확인 대기
- **§9 진행 세부(⚠️ 잠정)** — 타일 소진 후 말만 두기, 양쪽 불가 시 점수 종료, 한쪽만 불가 시
  패스. `design/rules.md §9`에 명시됨. 핫시트엔 영향 작음. 사용자가 다르게 원하면 조정.
