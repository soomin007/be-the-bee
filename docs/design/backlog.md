# 백로그 — 다음 작업 (진행 단일 소스)

> **앞으로 할 일·필요한 일만** 적는다. 이미 구현 완료됐거나 계획이 바뀐 항목은 지운다
> (무엇을 왜 했는지의 기록은 `session_logs/` 에만 남긴다). 큰 그림은 [`../ROADMAP.md`](../ROADMAP.md).
>
> 최종 갱신: 2026-06-24.

## 다음 마일스톤 후보 (우선순위 순)

> 인앱 3D 보드 렌더러(`src/ui/board3d.ts`)가 실험 토글로 들어가 있다(설정 → 3D 보드, 기본 꺼짐,
> 스타일/실사 2종). 다음 추천은 **#2(3D 완성도·기본값 승격 결정)**. 그 외는 모두 선택 항목.

| # | 작업 | 메모 |
|---|---|---|
| 2 | **3D 모드 완성도 점검 · 기본값 승격 여부**(사용자 결정) | 인앱 3D 렌더러는 구현됨 — `src/ui/board3d.ts`(타일·말·벌집 글로우 + 레이캐스팅 클릭 + 2D 와 동등한 오버레이: 직전 수·리치·승리 라인), 설정 토글 `board3d`(기본 꺼짐) + 말 스타일 `board3dStyle`(일반/실사). **남은 결정**: 2D 와 동등성 최종 확인 후 (a) 실험 딱지 떼고 정식 옵션화, (b) 기본값 승격 여부, (c) 스타일/실사 중 기본 스타일. 디자인 단일 출처: [`design_handoff_bee_pieces/`](../../design_handoff_bee_pieces/) · three.js 데모 2종: `public/three-demo.html`(스타일) / `public/three-demo-real.html`(실사). |
| 3 | **전문가 AI 추가 튜닝**(선택) | 전문가 티어는 적용됨(빔12·깊이4 + gapped-four + 해설, hard에 2-0-2 무패). 표본이 작아 `SIM_N` 키워 재검증 여지. 추가 강화 후보: 상대 동색 타일 런의 **열린 끝 조기 견제**, **여왕벌로 잠긴 칸 차단** — 모두 `npm run sim` 필드 A/B 필수 |
| 6 | **벌집 배경 패턴 잔여**(선택) | 배경 무늬·파비콘·OG 적용됨. 남은 건 커서/버튼 꿀색 톤 마무리 정도 |
| 7 | **멀티플레이 로비**(미래·범위 밖) | 초대링크 방 + 로비 RoomSettings 공통. 엔진 직렬화/스냅샷 준비됨(공유 코드 BTB1 포함). **CLAUDE.md상 네트워킹은 아직 안 만듦** |
