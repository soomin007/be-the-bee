# 백로그 — 다음 작업 (진행 단일 소스)

> **앞으로 할 일·필요한 일만** 적는다. 이미 구현 완료됐거나 계획이 바뀐 항목은 지운다
> (무엇을 왜 했는지의 기록은 `session_logs/` 에만 남긴다). 큰 그림은 [`../ROADMAP.md`](../ROADMAP.md).
>
> 최종 갱신: 2026-06-22.

## 다음 마일스톤 후보 (우선순위 순)

> 입력 없이 바로 가능한 추천은 **#1(저장 슬롯 확장)**. 나머지는 대부분 사용자 입력/판단 필요.

| # | 작업 | 메모 |
|---|---|---|
| 1 | **저장 슬롯 확장**(다음 추천) | 현재는 단일 저장 슬롯 + autosave. 여러 슬롯/이름·시간 표시·삭제, 기보 목록 UI. 입력 없이 진행 가능 |
| 2 | **AI 튜닝**(사용자 입력 필요) | 상성 분석은 [`ai_strategy.md`](ai_strategy.md)에 정리됨. hard 체감·벌집형 약함 조정은 실플레이 피드백 받아 `npm run sim`(필드 대결)으로 A/B |
| 3 | **말=벌 3D 모드 옵션**(방향 합의 필요) | 2.5D는 적용됨. 설정에 2D/3D 분기는 범위 큼 — 착수 전 사용자와 방향 합의 |
| 4 | **설정창 추가 다듬기**(선택) | 아코디언은 적용됨. 더 손본다면 아이콘·간격·반응형 등 세부 |
| 5 | **벌집 배경 패턴 잔여**(선택) | 배경 무늬·파비콘·OG 적용됨. 남은 건 커서/버튼 꿀색 톤 마무리 정도 |
| 6 | **멀티플레이 로비**(미래·범위 밖) | 초대링크 방 + 로비 RoomSettings 공통. 엔진 직렬화/스냅샷 준비됨. **CLAUDE.md상 네트워킹은 아직 안 만듦** |
| - | **AI 평가 최적화**(선택) | `evaluate` 가 노드마다 보드 풀스캔·findLines 를 여러 번 반복. owner 맵 1회 생성·공유로 단축 가능(현재 체감 지연 없음, 코드 감사 제안) |
