# 백로그 — 다음 작업 (진행 단일 소스)

> **앞으로 할 일·필요한 일만** 적는다. 이미 구현 완료됐거나 계획이 바뀐 항목은 지운다
> (무엇을 왜 했는지의 기록은 `session_logs/` 에만 남긴다). 큰 그림은 [`../ROADMAP.md`](../ROADMAP.md).
>
> 최종 갱신: 2026-06-30.

## 다음 마일스톤

> **다음 메인 = 멀티플레이어**(초대링크 온라인 대전). 그동안 "범위 밖"이었으나 사용자가 착수 결정.
> 엔진은 이미 직렬화·스냅샷·공유 코드(BTB1)가 준비돼 netcode 친화적이다. 방식·백엔드는 계획 문서에서
> 확정한다. (3D 보드는 정식 옵션으로 도입 완료 — 실험 딱지 제거, 실사 벌은 숨은 이스터에그.)

| # | 작업 | 메모 |
|---|---|---|
| 1 | **멀티플레이어 follow-up**(메인) | Phase 1~3 구현됨(Supabase 방·선공후공 협상·코인토스·이탈/재접속·재대국·온라인 무르기). 남은 일: **실기기 2대 검증**(협상 동기화·코인토스·온라인 무르기·만석 처리). '내 색 두 번' 데스크탑 해결, 모바일은 간헐적(known_issues 2026-06-26 참고). 계획: [`multiplayer_plan.md`](multiplayer_plan.md). |
| 2 | AI 잠긴 회랑 — **학습 value-net 으로 부분 돌파** | **학습 가치함수(value-net) 착수·진전**: 코드 접근(파라미터·정적·규칙·주도권)이 다 못 한 회랑 예방을 **value-net 이 게임1·2 회랑을 학습으로 예방**(off 휴리스틱은 못 함). 단독 leaf 는 강도 완패(0-6) → **blend 0.6 으로 강도 회복(vs hard 3-1)** + 회랑 부분(8수). 회랑↔강도 trade-off. **남은 일**: ① blend sweet spot 튜닝(0.7~0.8) + 큰 self-play(N↑) + 데이터 확대(빌더·대조쌍)로 균형 정밀화, ② 게임3(긴 회랑)은 피처-MLP 한계 → CNN/공간 인코딩(Stage 3, Python), ③ "전술 빈틈 0" 빔 안전망 하이브리드(후보). 상세: `session_logs/2026-06-30.md`, [`ai_hive_lock_defense.md`](ai_hive_lock_defense.md), 계획 `~/.claude/plans/federated-churning-grove.md`. |
