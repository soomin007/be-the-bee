# 백로그 — 다음 작업 (진행 단일 소스)

> **앞으로 할 일·필요한 일만** 적는다. 이미 구현 완료됐거나 계획이 바뀐 항목은 지운다
> (무엇을 왜 했는지의 기록은 `session_logs/` 에만 남긴다). 큰 그림은 [`../ROADMAP.md`](../ROADMAP.md).
>
> 최종 갱신: 2026-06-23.

## 다음 마일스톤 후보 (우선순위 순)

> 다음 추천 작업은 **#1(벌 비주얼 재작업)** — 사용자가 방향을 명확히 줬다(아래).

| # | 작업 | 메모 |
|---|---|---|
| 1 | **벌 비주얼 재작업 — 엎드린 탑다운 벌**(다음 추천, SVG+3D 공통) | **목표(실물 사진 기준)**: 벌이 **타일에 배를 대고 엎드림** → 플레이어(하늘) 시점에서 **등·날개·줄무늬**가 보이는 **위에서 내려다본** 모습. 요구: ① **원래 말 크기 유지**(키우지 말 것), ② **벌 상징성↑**(기존=선 2개라 빈약 / 현재=눈만 크고 날개 안 보이고 줄무늬 약함 — 둘 다 X), ③ **귀여움보다 "벌답게"** 우선, ④ 길쭉한 타원(머리+배), 검은 머리 + **작은** 눈, **흰 날개 등 위에 또렷이**, 검정/노랑 **줄무늬 또렷이**. **3D 현재 버전은 옆으로 누운 형태라 틀림** — 배가 바닥, 등이 위로. 참고: 실물 사진(갈색 디스크 위 위에서 본 벌). |
| 2 | **3D 모드 정식 도입 검토**(#1과 연관, 사용자 결정 대기) | **답**: three.js 로 **게임 전체 3D 구현 가능 — Blender 필수 아님**. 벌도 프리미티브(납작 타원체 등배 + 평평한 날개 + 줄무늬 텍스처)로 조립하면 사진 느낌 낼 수 있음(Blender GLTF 는 고품질 옵션일 뿐). **단 게임 본체 3D 전환 = 보드/타일/말 렌더 + 입력·히트테스트·카메라 재작성으로 큰 작업.** 데모 [`public/three-demo.html`]. 먼저 #1(벌 모양)을 데모에서 확정한 뒤 본체 도입 여부 결정 권장. |
| 3 | **전문가 AI 추가 튜닝**(선택) | 전문가 티어는 적용됨(빔12·깊이4 + gapped-four + 해설, hard에 2-0-2 무패). 표본이 작아 `SIM_N` 키워 재검증 여지. 추가 강화 후보(분석 2판째 근거): 상대 동색 타일 런의 **열린 끝 조기 견제**, **여왕벌로 잠긴 칸 차단** 고려 — 모두 `npm run sim` 필드 A/B 필수 |
| 5 | **설정창 다듬기 · 제작자 표기 세부**(선택) | 아코디언 적용됨. 아이콘·간격·반응형 세부 + 우하단 제작자 크레딧의 세부 디자인(최소판은 적용됨, 함께 진행하기로 함) |
| 6 | **벌집 배경 패턴 잔여**(선택) | 배경 무늬·파비콘·OG 적용됨. 남은 건 커서/버튼 꿀색 톤 마무리 정도 |
| 7 | **멀티플레이 로비**(미래·범위 밖) | 초대링크 방 + 로비 RoomSettings 공통. 엔진 직렬화/스냅샷 준비됨(공유 코드 BTB1 포함). **CLAUDE.md상 네트워킹은 아직 안 만듦** |
| - | **AI 평가 최적화**(선택) | `evaluate` 가 노드마다 보드 풀스캔·findLines 를 여러 번 반복(전문가 빔12·깊이4 + gapFour 로 ~3.7초/수). owner 맵 1회 생성·공유로 단축 가능 — 전문가 응답속도 개선 겸 |
