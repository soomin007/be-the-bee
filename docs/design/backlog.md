# 백로그 — 다음 작업 (진행 단일 소스)

> **앞으로 할 일·필요한 일만** 적는다. 이미 구현 완료됐거나 계획이 바뀐 항목은 지운다
> (무엇을 왜 했는지의 기록은 `session_logs/` 에만 남긴다). 큰 그림은 [`../ROADMAP.md`](../ROADMAP.md).
>
> 최종 갱신: 2026-06-30.

## 다음 마일스톤

> **다음 메인 = 멀티플레이어**(초대링크 온라인 대전). 그동안 "범위 밖"이었으나 사용자가 착수 결정.
> 엔진은 이미 직렬화·스냅샷·공유 코드(BTB1)가 준비돼 netcode 친화적이다. 방식·백엔드는 계획 문서에서
> 확정한다. (3D 보드는 정식 옵션으로 도입 완료 — 실험 딱지 제거, 실사 벌은 숨은 이스터에그.)

| # | 작업 | 메모 |
|---|---|---|
| 1 | **멀티플레이어 follow-up**(메인) | Phase 1~3 구현됨(Supabase 방·선공후공 협상·코인토스·이탈/재접속·재대국·온라인 무르기). 남은 일: **실기기 2대 검증**(협상 동기화·코인토스·온라인 무르기·만석 처리). '내 색 두 번' 데스크탑 해결, 모바일은 간헐적(known_issues 2026-06-26 참고). 계획: [`multiplayer_plan.md`](multiplayer_plan.md). |
| 2 | AI 잠긴 회랑 — **코드·학습(피처-MLP) 다 막힘, 사용자 판단 대기** | 코드 접근(파라미터·정적·규칙·주도권)에 이어 **학습 value-net(피처-MLP)도 ship 불가로 확정**. value-net 단독 leaf 는 회랑을 학습 예방하나 강도 완패(vs hard 0-6), blend 로 강도 회복하면 회랑 catch 가 무너진다 — N=5 측정에서 모든 blend 가 off(10-0) 대비 회귀(5~6/10). 근본은 `encodeFeatures` 의 공간 정보 부재(21 전역 스칼라). **남은 선택지(사용자 결정)**: ① **UX 코칭 폴백**(가장 실용 — 긴 회랑은 사람 상대 한정 약점, 코칭으로 "잠기기 전 끊기" 안내, 인프라 있음), ② **Stage 3 CNN/공간 인코딩**(Python, 큰 작업 — 강도 회귀 신호로 ROI 불확실), ③ 현 상태 수용(기본 expert=MCTS 가 vs hard 10-0 으로 강함, 사람-회랑 정석만 약점). value-net 은 옵션(`valueNet`)으로 보존·기본 off. 상세: [`ai_hive_lock_defense.md`](ai_hive_lock_defense.md) §3 "value-net 종합", `session_logs/2026-06-30.md`. |
