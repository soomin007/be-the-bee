# 백로그 — 다음 작업 (진행 단일 소스)

> **앞으로 할 일·필요한 일만** 적는다. 이미 구현 완료됐거나 계획이 바뀐 항목은 지운다
> (무엇을 왜 했는지의 기록은 `session_logs/` 에만 남긴다). 큰 그림은 [`../ROADMAP.md`](../ROADMAP.md).
>
> 최종 갱신: 2026-07-08.

## 다음 마일스톤

> **다음 메인 = 멀티플레이어**(초대링크 온라인 대전). 그동안 "범위 밖"이었으나 사용자가 착수 결정.
> 엔진은 이미 직렬화·스냅샷·공유 코드(BTB1)가 준비돼 netcode 친화적이다. 방식·백엔드는 계획 문서에서
> 확정한다. (3D 보드는 정식 옵션으로 도입 완료 — 실험 딱지 제거, 실사 벌은 숨은 이스터에그.)

| # | 작업 | 메모 |
|---|---|---|
| 1 | **멀티플레이어 follow-up**(메인) | Phase 1~3 구현됨(Supabase 방·선공후공 협상·코인토스·이탈/재접속·재대국·온라인 무르기). 남은 일: **실기기 2대 검증**(협상 동기화·코인토스·온라인 무르기·만석 처리). '내 색 두 번' 데스크탑 해결, 모바일은 간헐적(known_issues 2026-06-26 참고). 계획: [`multiplayer_plan.md`](multiplayer_plan.md). |
| 2 | AI 잠긴 회랑 — **UX 코칭 폴백 적용 완료, AI 측은 보류** | 코드(파라미터·정적·규칙·주도권)·학습(value-net 피처-MLP) 접근이 다 ship 불가로 확정(근본: `encodeFeatures` 공간 정보 부재). **사용자 선택으로 UX 코칭 폴백 적용**: '강하게' 코칭이 곧 잠길 상대 벌집 줄을 잠기기 전에 끊으라고 안내(`corridorLockThreats` + 청록 강조/문구). 핫시트 방어자에게 특히 유용. **AI 가 회랑에 지는 문제 자체는 미해결**(Stage 3 CNN/공간 인코딩 — 큰 작업, 강도 회귀 ROI 불확실, **보류**; 사용자가 원하면 착수). value-net 은 옵션(`valueNet`)으로 보존·기본 off. 상세: [`ai_hive_lock_defense.md`](ai_hive_lock_defense.md) §3, `session_logs/2026-06-30.md`. **재개 시 1순위 실험(싼 것부터, 2026-07-07 관찰)**: 06-30 MCTS 그리드 프로브에서 "회랑 끊는 수"가 후보에 들어갔는지 미확인 — `mctsActions` 는 즉시차단·열린3목·잠금수만 시딩하고 끊는 수는 시딩 안 함(known_issues 06-29 "후보에 없는 수는 못 둔다"의 방어판 재현 가능성). 이미 있는 `corridorLockThreats` 의 cutCells 를 MCTS 루트에 시딩(강제 아님)해 게임3 프로브 + self-play N≥5 강도 재검증 — 1세션짜리 실험. |
