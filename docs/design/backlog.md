# 백로그 — 다음 작업 (진행 단일 소스)

> **앞으로 할 일·필요한 일만** 적는다. 이미 구현 완료됐거나 계획이 바뀐 항목은 지운다
> (무엇을 왜 했는지의 기록은 `session_logs/` 에만 남긴다). 큰 그림은 [`../ROADMAP.md`](../ROADMAP.md).
>
> 최종 갱신: 2026-06-27.

## 다음 마일스톤

> **다음 메인 = 멀티플레이어**(초대링크 온라인 대전). 그동안 "범위 밖"이었으나 사용자가 착수 결정.
> 엔진은 이미 직렬화·스냅샷·공유 코드(BTB1)가 준비돼 netcode 친화적이다. 방식·백엔드는 계획 문서에서
> 확정한다. (3D 보드는 정식 옵션으로 도입 완료 — 실험 딱지 제거, 실사 벌은 숨은 이스터에그.)

| # | 작업 | 메모 |
|---|---|---|
| 1 | **멀티플레이어 follow-up**(메인) | Phase 1~3 구현됨(Supabase 방·선공후공 협상·코인토스·이탈/재접속·재대국·온라인 무르기). 남은 일: **실기기 2대 검증**(협상 동기화·코인토스·온라인 무르기·만석 처리). '내 색 두 번' 데스크탑 해결, 모바일은 간헐적(known_issues 2026-06-26 참고). 계획: [`multiplayer_plan.md`](multiplayer_plan.md). |
| 2 | AI "타일 벌집 잠금→말 채우기" 패턴 — **공격·방어 둘 다 다뤄짐, 남은 건 MCTS 제품화** | 사람이 **벌집을 잠그고 잠긴 칸을 말로 채워 막을 수 없는 5목**을 만드는 정석. **공격 절반**(자기 회랑 활용)은 2026-06-29 `lockedRun`(전문가)으로, **방어 절반**(잠기기 전 예방)은 **MCTS 엔진**(`engine:'mcts'`, 실험·기본 OFF)으로 다뤘다 — MCTS 가 게임1·2 회랑을 잠김 전에 선점하고 expert 대비 self-play 10-2. **남은 일**: MCTS 의 **UI 노출 방식 결정**(새 티어 vs expert 교체 vs 실험 유지 — 속도 ~5s/수 트레이드오프), 더 큰 self-play 로 강도/속도 튜닝, 갈색 후공 2패 분석. 상세: [`ai_strategy.md`](ai_strategy.md) "MCTS 엔진" · [`ai_hive_lock_defense.md`](ai_hive_lock_defense.md). |
| 3 | **기보 분석 — 추천 수·점수 손해 확장** | 복기 "이 판 분석"(수별 분류 코칭 + 결정적 순간 요약, 클릭 점프)은 구현됨(engine `analyzeGame`). **다음 단계**: 각 수에 "최선 수 대비 점수 손해(centipawn loss)"와 "더 나은 수 추천"을 더한다. `ai.ts` 의 `evaluate`/`negamax`/`generateCandidates`(현재 비공개)를 분석용으로 export 하거나 래퍼 신설 필요. 수마다 탐색이라 무거움 → 진입 시 1회 계산·캐싱(현재는 렌더마다 `analyzeGame` 재계산), 긴 기보는 비동기/청크 고려. |
