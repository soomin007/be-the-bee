# 백로그 — 다음 작업 (진행 단일 소스)

> **앞으로 할 일·필요한 일만** 적는다. 이미 구현 완료됐거나 계획이 바뀐 항목은 지운다
> (무엇을 왜 했는지의 기록은 `session_logs/` 에만 남긴다). 큰 그림은 [`../ROADMAP.md`](../ROADMAP.md).
>
> 최종 갱신: 2026-06-22.

## 다음 마일스톤 후보 (우선순위 순)

> 대부분 사용자 판단/실플레이 피드백이 필요하다. 입력 없이 바로 손볼 수 있는 건
> **#2(설정창 다듬기 · 제작자 표기 세부)** 정도.

| # | 작업 | 메모 |
|---|---|---|
| 1 | **AI 리치 견제 고도화 — hard 전용**(합의됨, 미착수) | **벌집 회랑 견제**: gapped-four(`X·XXX`) 위협 인식 + 상대 잠재 벌집 라인 방어를 **hard 난이도에만** 적용(easy/medium 은 지금처럼). 분석 근거 [`ai_strategy.md`](ai_strategy.md). 구현 후 `npm run sim`(필드 대결) A/B 로 회귀·과적합 확인 |
| 2 | **3D 모드 / 토큰 비주얼**(검토 중) | SVG 실물 토큰(두께 타일+디스크+돔 벌)은 적용됨. three.js 3D 시연은 [`public/three-demo.html`](../../public/three-demo.html)(`npm run dev` → `/three-demo.html`). 풀 3D 모드 도입 여부는 사용자 결정 대기 — 도입 시 SVG 보드와 별개 렌더 경로/의존성 추가 검토 |
| 3 | **설정창 다듬기 · 제작자 표기 세부**(선택) | 아코디언 적용됨. 아이콘·간격·반응형 세부 + 우하단 제작자 크레딧의 세부 디자인(최소판은 적용됨, 함께 진행하기로 함) |
| 4 | **벌집 배경 패턴 잔여**(선택) | 배경 무늬·파비콘·OG 적용됨. 남은 건 커서/버튼 꿀색 톤 마무리 정도 |
| 5 | **멀티플레이 로비**(미래·범위 밖) | 초대링크 방 + 로비 RoomSettings 공통. 엔진 직렬화/스냅샷 준비됨(공유 코드 BTB1 포함). **CLAUDE.md상 네트워킹은 아직 안 만듦** |
| - | **AI 평가 최적화**(선택) | `evaluate` 가 노드마다 보드 풀스캔·findLines 를 여러 번 반복. owner 맵 1회 생성·공유로 단축 가능(현재 체감 지연 없음, 코드 감사 제안) |
